<UserControl x:Class="LaserCleanChamber.View.Controls.ChamberView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LaserCleanChamber.View.Controls"
             xmlns:svgs="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--<svgs:SvgViewbox Grid.Row="1" Source="SVG/Body.svg"/>-->

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <RadioButton x:Name="r1" IsChecked="True"/>
            <RadioButton x:Name="r2"/>
            <RadioButton x:Name="r3"/>
            <RadioButton x:Name="r4"/>
            <RadioButton x:Name="r5"/>
        </StackPanel>

        <ContentControl Grid.Row="1" Visibility="Visible">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=r1, Path=IsChecked, UpdateSourceTrigger=PropertyChanged}" Value="True">
                            <Setter Property="Content" Value="{StaticResource ChamberStateClosed}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=r2, Path=IsChecked, UpdateSourceTrigger=PropertyChanged}" Value="True">
                            <Setter Property="Content" Value="{StaticResource ChamberStateClosedLaser}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=r3, Path=IsChecked, UpdateSourceTrigger=PropertyChanged}" Value="True">
                            <Setter Property="Content" Value="{StaticResource ChamberStateClosedLaserObject}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=r4, Path=IsChecked, UpdateSourceTrigger=PropertyChanged}" Value="True">
                            <Setter Property="Content" Value="{StaticResource ChamberStateOpenedLaserObject}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=r5, Path=IsChecked, UpdateSourceTrigger=PropertyChanged}" Value="True">
                            <Setter Property="Content" Value="{StaticResource ChamberStateOpenedObject}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>

        </ContentControl>

        <TextBlock Grid.Row="2" Margin="4" FontWeight="Bold" HorizontalAlignment="Center" FontSize="16">
                    <Run Text="Состояние камеры:" />
                    <Run Text="Открыта" Foreground="Tomato" x:Name="chamber_state"/>
        </TextBlock>
    </Grid>

</UserControl>
